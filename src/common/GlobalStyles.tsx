import React from 'react';

import { Common, Markdown, Reset } from './styles';

const GlobalStyles = () => (
  <>
    <Reset />
    <Common />
    <Markdown />
  </>
);

export default GlobalStyles;
